1.) Load in the links the the latest live text and write to SQL
  1.1) can now look at the plots of the last weeks xG v goals
2.) load in the odds for the next gameweek and write to SQL
3.) fit the model
4.) write the model predictions for the next gameweek to SQL
5.) run the Markdown to knit the predictions to the HTML doc and review